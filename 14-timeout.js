/*

    TimeOut

    Setting timeout helps the page to stop loading and dispaly error if it has
    exceeded the specified time.
    The time is calculated in milliseconds.


Example 1:

axios
    .get("https://jsonplaceholder.typicode.com/todos", {
       timeout: 5000
     })
 */